import * as ComicFont from "@remotion/google-fonts/Bangers"
import * as SansSerifFont from "@remotion/google-fonts/SpaceGrotesk"
const { fontFamily: comicFont } = ComicFont.loadFont()
const { fontFamily: sansSerifFont } = SansSerifFont.loadFont()

export { comicFont, sansSerifFont }
